# Rich Text Editor API

Exposes sanitization, preparation/accessibility checks, and safe HTTP(S) embed generation through the API registry. All markup handling delegates to the provider-neutral domain service.
